package conditionals

/* Check the age by using if statement. */

fun checkAge(age: Int) {
    if (age < 18) {
        println("You are under age!")
    } else {
        println("You are good to go!")
    }
}